/* Complete the method that takes a boolean value and return a "Yes" string for true, or a "No" string for false. */
public class ConvertBooleans {
    public static String boolToWord(boolean boo) {
        if (boo == true) {
            return "Yes";
        } else {
            return "No";
        }
    }
}
